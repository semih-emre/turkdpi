# nfqws motoru

Bu dizine üçüncü taraf ikili dosya commit edilmez. `PKGBUILD`, resmi Zapret deposundaki `v72.10` sürümünü tam `f0b0d89f02f44bb047fbfde5d96e9a1fc38e46f0` commit kimliğiyle indirir, `nfq/Makefile` ile derler ve `/usr/lib/turkdpi/nfqws` konumuna kurar.

Elle kurulum betiğini kullanacaksanız aynı kaynağı derleyip ortaya çıkan `nfqws` dosyasını `engine/nfqws` olarak yerleştirin. Kaynağın MIT lisansı paket içinde ayrıca kurulur.
